import styled from "styled-components";

export const Wrapper = styled.div`
  gap: 4px;
  color: #274060;

  strong {
    font-size: 12px;
    font-weight: 700;
    text-transform: lowercase;
  }
  p {
    font-size: 14px;
  }
`;
